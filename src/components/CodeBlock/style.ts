import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    padding: 16,
    // flexShrink: 1,
    // margin: 8,
  },
  code: {
    fontFamily: "Courier",
    backgroundColor: "#63627C",
    padding: 12,
    borderRadius: 6,
  },
});
