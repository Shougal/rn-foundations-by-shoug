import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    padding: 16,
    backgroundColor: "#f0f0f0",
    borderRadius: 12,
    flexShrink: 1,
  },
  header: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 18,
    marginBottom: 8,
  },
});
